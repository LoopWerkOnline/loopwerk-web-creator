# Afspraken voor agents

- Hosting en deploy lopen alleen via Vercel (push naar `main` = deploy). Lovable wordt niet meer gebruikt.
- Geen force-push of history rewrites op `main`.
- Zie `CLAUDE.md` voor context, positionering en techniek.
